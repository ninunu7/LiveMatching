   <div class="container">
     <h1 class="top-title">ようこそ！！</h1>
      <p class="top-text">このサイトは、一緒にアーティストのライブ・コンサートに参加する人を探したり<br>交流したりする事ができます！</p>
      <% if customer_signed_in? %>
        <div class="top-button">
          <%= link_to "検索する",events_search_path, class: "btn btn-orange btn-orange-shadow" %>
        </div>
        <div class="top-button">
          <%= link_to "ログアウト",destroy_customer_session_path, method: :delete, class: "btn btn-orange btn-orange-shadow" %>
        </div>
        <h2 class="new-blog">新着のブログ</h2>
          <% if @blogs.exists? %>
            <table class="table table-hover text-center">
              <thead>
                <tr>
                  <th colspan="2">ユーザー</th>
                  <th>タイトル</th>
                  <th class="top-none">本文</th>
                  <th></th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <% @blogs.each do |blog| %>
                  <tr>
                    <td><%= image_tag blog.customer.profile_image.to_s, :size =>'50x50' %></td>
                    <td><%= link_to(blog.customer.name, customer_path(blog.customer)) %></td>
                    <td><%= blog.blog_title.truncate(15) %></td>
                    <td class="top-none"><%= blog.blog_text.truncate(15) %></td>
                    <td><%= link_to '詳細を見る', blog_path(blog), class: "btn btn-sm btn-primary" %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          <% else %>
              <p>新着ブログはありません</p>
          <% end %>

      <% else %>
        <div class="top-button">
          <%= link_to "検索する",events_search_path, class: "btn btn-orange btn-orange-shadow" %>
        </div>
        <div class="top-button">
          <%= link_to "新規登録",new_customer_registration_path, class: "btn btn-orange btn-orange-shadow" %>
        </div>
        <div class="top-button">
          <%= link_to "ログイン",new_customer_session_path, class: "btn btn-orange btn-orange-shadow" %>
        </div>
        <div class="admin">管理者の方はこちら</div>
          <div class="top-button">
            <%= link_to "管理者ログイン",new_admin_session_path, class: "btn btn-orange btn-orange-shadow" %>
          </div>
      <% end %>
    </div>

